//// Created by dominichann on 4/28/24.//#include <iostream>#include "../Game.h"#include "../TextureManager.h"#include "../Handler.h"#include "../PlayerBase.h"#include "../MissileExplo.h"#include <libAssets.h>#include <random>int Game::nextID = 0;Handler *Game::handler = nullptr;AudioSystem *Game::audioSystem = nullptr;bool Game::showBoaders = false;Game::Game() {    background = nullptr;    playerBase = nullptr;    cursorG = nullptr;    missilesLoaded = 10;    stacksLeft = 3;    gameOver = false;    currentDiffculty = EASY;    currentRound = 0;    roundInPlay = false;    launcher = new EnemyMissileLauncher(nullptr, 0, 0, new Timer(5), this);}Game::~Game() = default;SDL_Renderer *Game::renderer = nullptr;SDL_Window *Game::window = nullptr;void Game::init(const char *title, int xPos, int yPos, int width, int height, bool fullscreen) {    isRunning = false;    if ((SDL_Init(SDL_INIT_EVERYTHING)) == 0) {        SDL_Log("SubSystem Initialing");        int flags = 0;        if (fullscreen)            flags = SDL_WINDOW_FULLSCREEN;        window = SDL_CreateWindow(title, xPos, yPos, width, height, flags);        if (window == nullptr) {            SDL_Log("Failed to create window: %s", SDL_GetError());            clean();        }        renderer = SDL_CreateRenderer(window, -1, SDL_RENDERER_ACCELERATED);        if (renderer == nullptr) {            SDL_Log("Failed to create renderer: %s", SDL_GetError());            SDL_DestroyWindow(window);            SDL_Quit();            clean();        } else            SDL_Log("Created renderer and Window");        isRunning = true;        if (!initAssets("assets.darw")) {            SDL_Log("Failed to load assets archive: %s", SDL_GetError());            SDL_DestroyWindow(window);            SDL_Quit();            clean();            exit(1);        }        // 1. FIX: Check if asset info loaded correctly (Checks SHA256 and validity)        info assetMetadata = loadAssetsInfo();        if (assetMetadata.NAME == "NULL") {            SDL_Log("Critical Error: Asset integrity check failed! (Corrupt file or bad Checksum)");            SDL_DestroyWindow(window);            SDL_DestroyRenderer(renderer);            SDL_Quit();            clean();            exit(1); // Stop the program immediately        }        handler->addObject(launcher);        listAssets();        // 2. FIX: specific asset loader helper        // This lambda function checks every asset request. If "NULL" comes back, it crashes gracefully.        // Change return type to std::string        auto safeGetAsset = [&](const std::string &path) -> std::string {            std::pair<std::string, std::string> asset = getAsset(path);            if (asset.first == "NULL") {                SDL_Log("Critical Error: Missing or unreadable asset: %s", path.c_str());                clean();                exit(1);            }            return asset.first; // Return the string object by value (copy/move)        };        // Now use safeGetAsset() instead of getAsset().first.c_str()        // This makes the code cleaner and safer.        SDL_Surface *icon = SDL_LoadBMP(safeGetAsset("images/icon.bmp").c_str());        if (!icon) {            SDL_Log("Failed to create surface for icon: %s", SDL_GetError());            // Handle error...        }        SDL_SetWindowIcon(window, icon);        SDL_SetRenderDrawColor(renderer, 255, 255, 255, 255);        SDL_Log("SubSystem Initialed");        cursorG = new Cursor(safeGetAsset("images/cursor.png").c_str(), 100, 100);        background = new Background(safeGetAsset("images/back.bmp").c_str());        // ... rest of your code using safeGetAsset ...        playerBase = new PlayerBase(nullptr, PLAYERBASE_X, PLAYERBASE_Y, cursorG);        Game::handler->addObject(playerBase);        Game::handler->addObject(cursorG);        audioSystem = new AudioSystem();        //audioSystem->LoadMP3(safeGetAsset("sfx/back.mp3"));        listAssets();        for (int i = 0; i < 3; i++) {            // safeGetAsset makes this line much easier to read too!            City *temp = new City(safeGetAsset("images/city.png").c_str(), (i * 180) + 30, 550,                                  safeGetAsset("images/destCity.png").c_str(), i);            Game::handler->addObject(temp);            cities.push_back(temp);        }        for (int i = 3; i < 6; i++) {            City *temp = new City(safeGetAsset("images/city.png").c_str(), (i * 180) + 190, 550,                                  safeGetAsset("images/destCity.png").c_str(), i);            Game::handler->addObject(temp);            cities.push_back(temp);        }    }}void Game::handleEvents() {    SDL_Event event;    static bool f3Pressed = false;    while (SDL_PollEvent(&event)) {        Game::handler->handleEvents(&event);        switch (event.type) {            case SDL_QUIT:                isRunning = false;                break;            case SDL_KEYDOWN:                if (event.key.keysym.sym == SDLK_F3) {                    f3Pressed = true;                } else if (event.key.keysym.sym == SDLK_b && f3Pressed) {                    // F3 + B pressed                    Game::showBoaders = !Game::showBoaders; // Toggle showBorders                    std::cout << "F3 + B pressed. ShowBorders: " << (Game::showBoaders ? "On" : "Off") << std::endl;                }                break;            case SDL_KEYUP:                if (event.key.keysym.sym == SDLK_F3) {                    f3Pressed = false;                } else if (event.key.keysym.sym == SDLK_t) {                    launcher->StartMissile(EASY);                }                break;            default:                break;        }    }}void Game::update() {    background->Update(); // update background    Game::handler->Update();    Game::checkGameStatus();    Game::checkRoundStart();}void Game::render() {    SDL_RenderClear(renderer);    background->Render(); // render background    Game::handler->Render();    SDL_RenderPresent(renderer);}void Game::testLaunch() {    launcher->StartMissile(EASY);}void Game::clean() {    SDL_DestroyWindow(window);    SDL_DestroyRenderer(renderer);    releaseAssetsFile();    SDL_Quit();    SDL_Log("Game cleaned and Closed!");}bool Game::running() const {    return isRunning;}void Game::missileShot() {    missilesLoaded--;}void Game::reload() {    missilesLoaded = 10;}short Game::missilesLeft() const {    return missilesLoaded;}void Game::checkMissileCol(MissileExplo *explo) {    for (auto *gameObject: handler->GameObjects) {        auto *missile = dynamic_cast<Missile *>(gameObject);        if (missile) {            if (!missile->isFriendly()) {                if (missile->CheckCollision(explo)) {                    missile->explode();                }            }        }    }}void Game::checkCity(MissileExplo *explo) {    for (auto *gameObject: handler->GameObjects) {        auto *cities = dynamic_cast<City *>(gameObject);        if (cities) {            {                if (cities->CheckCollision(explo)) {                    cities->explode();                }            }        }    }}void Game::checkCity(Missile *missile) {    for (auto *gameObject: handler->GameObjects) {        auto *cities = dynamic_cast<City *>(gameObject);        if (cities) {            {                if (cities->CheckCollision(missile)) {                    cities->explode();                    handler->removeObject(missile);                }            }        }    }}void Game::checkGameStatus() {    bool gameOverCheck = true;    for (auto city: cities) {        if (!city->isHit())            gameOverCheck = false;    }    if (gameOverCheck) {        isRunning = false;        gameOver = DEATH_BY_CITY;    }}short Game::getGameStatus() const {    return gameOver;}void Game::startRound() {    currentRound++;    switch (currentRound) {        case 1:        case 2:        case 3:        case 4:        case 5:        case 6:            currentDiffculty = EASY;            break;        case 7:        case 8:        case 9:        case 10:        case 11:        case 12:            currentDiffculty = MEDIUM;            break;        case 13:        case 14:        case 15:        case 16:        case 17:        case 18:            currentDiffculty = HARD;            break;        default:            currentDiffculty = IMMPOSSABLE;    }    launcher->StartMissile(currentDiffculty);    roundInPlay = true;}void Game::checkRoundStart() {    //Add check if score is counting and what not    if (!roundInPlay) {        startRound();    }}int Game::randomNumberGen(int a, int b) {    std::random_device rd; // Seed for the random number engine    std::mt19937 gen(rd()); // Mersenne Twister engine    // Define the range [0, 700]    std::uniform_int_distribution<> distrib(a, b);    // Generate a random number between 0 and 700    return distrib(gen);}City *Game::getCity(int num) {    if (num > 5 || num < 0)        return nullptr;    return cities[num];}bool Game::isValid() {    return window != nullptr && renderer != nullptr;}